# =============================================================================
# PaperSearch 项目环境配置文件模板
# =============================================================================
# 复制此文件为 .env 并填入实际配置值

# -----------------------------------------------------------------------------
# 后端 Flask 应用配置
# -----------------------------------------------------------------------------
# Flask 服务器配置
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=false
FLASK_ENV=production

# -----------------------------------------------------------------------------
# 前端 Vite 开发服务器配置
# -----------------------------------------------------------------------------
# Vite 开发服务器端口
VITE_DEV_PORT=5173
# API 代理目标地址
VITE_API_PROXY_TARGET=http://localhost:5000

# -----------------------------------------------------------------------------
# MongoDB 数据库配置
# -----------------------------------------------------------------------------
# MongoDB 连接参数
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_USER=your_username
MONGO_PASSWORD=your_password
MONGO_DATABASE=papersearch

# MongoDB 连接字符串 (可选，优先级高于单独参数)
# MONGO_URI=mongodb://username:password@localhost:27017/papersearch

# -----------------------------------------------------------------------------
# 日志配置
# -----------------------------------------------------------------------------
# 日志目录
LOG_DIR=logs
# 日志保留天数
LOG_RETENTION_DAYS=30
# 日志文件大小限制
LOG_MAX_SIZE=100MB
# 日志级别 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# API 服务配置
# -----------------------------------------------------------------------------
# API 基础 URL（用于前端调用）
API_BASE_URL=http://localhost:5000
# API 版本
API_VERSION=v1
# CORS 允许的来源
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# -----------------------------------------------------------------------------
# 外部 API 配置
# -----------------------------------------------------------------------------
# Gemini API 密钥 (多个密钥用逗号分隔)
GEMINI_API_KEYS=your_api_key_1,your_api_key_2

# Gemini API 提示语配置
GEMINI_PROMPT_DEFAULT=用中文讲一下这篇文章的内容，并举一个例子说明问题和方法流程。

GEMINI_PROMPT_DETAILED=请以文章作者的身份，深入剖析这篇文章，详细回顾您从选题到提出方法解决问题整个思维决策过程。

# -----------------------------------------------------------------------------
# 生产环境配置
# -----------------------------------------------------------------------------
# 生产环境域名
PRODUCTION_HOST=
PRODUCTION_PORT=80

# 生产环境 MongoDB 配置
PRODUCTION_MONGO_HOST=
PRODUCTION_MONGO_PORT=27017
PRODUCTION_MONGO_USER=
PRODUCTION_MONGO_PASSWORD=
PRODUCTION_MONGO_DATABASE=papersearch

# -----------------------------------------------------------------------------
# 开发配置开关
# -----------------------------------------------------------------------------
# 是否启用开发模式
DEV_MODE=true
# 是否显示详细错误信息
SHOW_ERROR_DETAILS=true
# 是否启用 API 日志
ENABLE_API_LOGGING=true